package com.ferret.platform

import com.ferret.FerretConfiguration

internal actual fun bootServices(configuration: FerretConfiguration) {
    // no-op on native — add platform services here when needed
}
